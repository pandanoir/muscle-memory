# 筋トレ記録アプリ 要件定義

## 概要

ChatGPTと組み合わせて使うことを前提とした、筋トレ記録専用のシンプルなアプリを作成する。

このアプリ自体は分析やメニュー生成などは一切行わず、「筋トレ記録の永続化」と「ChatGPTとのデータ受け渡し」のみを担当する。

---

# コンセプト

- AI機能は持たない
- 筋トレ記録はすべてテキストとして保存する
- ChatGPTが毎回出力する「リザルト」を保存するだけ
- アプリはメモ帳＋クリップボードのような役割

---

# 想定フロー

## 1. アプリを開く

保存済みの筋トレ履歴（プロンプト）を取得する。

ユーザーは

- 「ChatGPTへコピー」

ボタンを押す。

これにより保存済みのテキストがクリップボードへコピーされる。

（将来的にはChatGPTを起動するボタンを追加してもよい。）

---

## 2. ChatGPTでやること

ユーザーがChatGPTへ貼り付ける。

その後

- 今日のメニューを生成
- 今日のトレーニング内容を送信
- 最後に「リザルト」と入力

するとChatGPTが

- 過去の記録
- 今日の記録

を統合した新しい履歴を出力する。

---

## 3. アプリへ戻る

ユーザーはChatGPTの「リザルト」をコピーする。

アプリで

- 「貼り付け」

ボタンを押す。

クリップボードの内容を読み取り、そのまま保存する。

これで次回以降は新しい履歴が利用される。

---

# 保存するデータ

DBには以下のみ保存する。

```text
id
updatedAt
content
```

content は ChatGPT が出力した全文をそのまま保存する。

内部で解析・分解・構造化は行わない。

SQLiteなどローカルDBで十分。

---

# UI

画面は1画面のみ。

表示内容

- 最終更新日時
- 保存済み文字数（任意）
- 保存済み内容のプレビュー（任意）

ボタン

- ChatGPTへコピー
- 貼り付けして保存

以上。

余計な画面は作らない。

---

# 「ChatGPTへコピー」

押したら

1. 保存済みテキストを取得
2. クリップボードへコピー
3. コピー完了を通知

---

# 「貼り付けして保存」

押したら

1. クリップボードを取得
2. テキストであることを確認
3. 保存
4. 更新日時を更新
5. 保存完了を通知

---

# 保存データ例

```text
これは筋トレの記録です。

日付、種目、回数、インターバルを記載しています。

「今日のメニュー」と言われたら、
過去の記録を参考に本日のトレーニングメニューを提案してください。

「リザルト」と言われたら、
今回の内容を反映した最新版を、この形式のまま全文出力してください。

=====

2026-07-20

ベンチプレス インターバル2分
60kg ×10, 60kg ×10, 60kg x8

...

2026-07-25

...
```

アプリはこの内容を理解しなくてよい。

単なる文字列として扱う。

---

# 設計方針

- AI処理は禁止
- パース禁止
- JSON化禁止
- 独自フォーマットへの変換禁止

あくまで「ChatGPTが生成したテキストをそのまま保存する」。

---

# 技術要件

- ローカル保存（SQLite推奨）
- オフライン動作
- データは1件のみ保持（常に最新を上書き）
- シンプルな構成を優先
- 保守性を重視し、過度なアーキテクチャは不要

---

# 将来的な拡張（今回は不要）

- ChatGPTアプリの起動
- OpenAI API対応
- MCP連携
- クラウド同期
- バックアップ／エクスポート
- 履歴管理（複数世代保存）
- Markdown表示
- 検索機能

今回はこれらは実装しない。

まずは「ChatGPTとのテキストの受け渡し」を最小構成で実現することを目的とする。
